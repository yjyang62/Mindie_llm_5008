# 卸载

用户可通过卸载whl软件包完成卸载，本章以卸载MindIE LLM为例。

执行如下命令，卸载whl包，完成卸载。

```
pip uninstall mindie_llm-{version}-{python tag}-{abi tag}-{platform tag}.whl

```
> [!NOTE]说明
> 上方以mindie_llm包为例，如卸载MindIE Motor或MindIE SD，请替换为对应的whl包名。

卸载完成后，若打印如下信息，则说明软件卸载成功：

```

[INFO] XXX uninstall success

```